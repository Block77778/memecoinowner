const fs = require("fs"); const path = require("path"); export default function handler(req, res) { const { txHash } = req.query; const filePath = path.join(process.cwd(), "transactions.json"); if (!fs.existsSync(filePath)) return res.json({ status: "not_found" }); const transactions = JSON.parse(fs.readFileSync(filePath, "utf-8")); const tx = transactions.find((t) => t.txHash === txHash); if (!tx) return res.json({ status: "not_found" }); return res.json({ status: tx.status }); }
